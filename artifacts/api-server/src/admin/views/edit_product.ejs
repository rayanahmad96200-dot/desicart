<%- include('_header', { title: 'Edit Product — DesiCart Admin', pageTitle: 'Edit Product', username, flash, currentPath }) %>

<div class="row justify-content-center">
    <div class="col-lg-8 col-xl-7">
        <div class="d-flex align-items-center gap-2 mb-3">
            <a href="/admin-panel/dashboard" class="btn btn-sm btn-light" style="border-radius:8px;"><i class="bi bi-arrow-left"></i> Back</a>
            <h5 class="mb-0 fw-bold">Edit: <%= product.title %></h5>
        </div>
        <div class="card">
            <div class="card-body p-4">
                <form method="POST" action="/admin-panel/products/edit/<%= product.id %>" enctype="multipart/form-data">

                    <div class="mb-4">
                        <label class="form-label">Product Image</label>
                        <div id="dropzone" class="text-center p-4" style="border:2px dashed #e2e8f0;border-radius:12px;cursor:pointer;background:#fafafa;transition:all 0.2s;"
                            onclick="document.getElementById('imageInput').click()"
                            ondragover="event.preventDefault();this.style.borderColor='#e07b2a';"
                            ondragleave="this.style.borderColor='#e2e8f0';"
                            ondrop="handleDrop(event)">
                            <% if (product.image_path) { %>
                            <div id="dropzoneContent" style="display:none;">
                                <i class="bi bi-cloud-upload" style="font-size:2.5rem;color:#cbd5e1;"></i>
                                <p class="mb-1 mt-2" style="color:#64748b;font-weight:500;">Click to upload or drag & drop</p>
                            </div>
                            <img id="previewImg" src="/admin-panel/static/uploads/<%= product.image_path %>" style="max-height:200px;border-radius:10px;object-fit:contain;">
                            <% } else { %>
                            <div id="dropzoneContent">
                                <i class="bi bi-cloud-upload" style="font-size:2.5rem;color:#cbd5e1;"></i>
                                <p class="mb-1 mt-2" style="color:#64748b;font-weight:500;">Click to upload or drag & drop</p>
                                <p class="mb-0" style="font-size:0.8rem;color:#94a3b8;">PNG, JPG, JPEG, GIF, WEBP — max 16MB</p>
                            </div>
                            <img id="previewImg" src="" style="display:none;max-height:200px;border-radius:10px;object-fit:contain;">
                            <% } %>
                        </div>
                        <input type="file" name="image" id="imageInput" accept="image/*" style="display:none;" onchange="previewImage(this)">
                        <% if (product.image_path) { %>
                        <div class="text-muted small mt-2"><i class="bi bi-image me-1"></i>Current image shown — upload new to replace</div>
                        <% } %>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Product Title <span class="text-danger">*</span></label>
                        <input type="text" name="title" class="form-control" required value="<%= product.title %>">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Tagline <span class="text-muted">(short subtitle)</span></label>
                        <input type="text" name="tagline" class="form-control" value="<%= product.tagline || '' %>">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Description</label>
                        <textarea name="description" class="form-control" rows="3"><%= product.description || '' %></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Features <span class="text-muted">(one per line)</span></label>
                        <textarea name="features" class="form-control" rows="5"><%= featuresText %></textarea>
                    </div>

                    <div class="row g-3 mb-3">
                        <div class="col-sm-6">
                            <label class="form-label">Sale Price (Rs.) <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text" style="border-radius:8px 0 0 8px;background:#f8fafc;">Rs.</span>
                                <input type="number" name="price" class="form-control" step="0.01" min="0" required value="<%= product.price %>" style="border-radius:0 8px 8px 0;">
                            </div>
                        </div>
                        <div class="col-sm-6">
                            <label class="form-label">Original Price (Rs.) <span class="text-muted">(optional)</span></label>
                            <div class="input-group">
                                <span class="input-group-text" style="border-radius:8px 0 0 8px;background:#f8fafc;">Rs.</span>
                                <input type="number" name="original_price" class="form-control" step="0.01" min="0" value="<%= product.original_price || '' %>" style="border-radius:0 8px 8px 0;">
                            </div>
                        </div>
                    </div>

                    <div class="row g-3 mb-4">
                        <div class="col-sm-6">
                            <label class="form-label">Category</label>
                            <select name="category" class="form-select">
                                <option value="">Select category</option>
                                <% for (const cat of ['Smart Watches', 'Earbuds', 'Speakers', 'Power Banks', 'Headphones', 'Accessories']) { %>
                                <option value="<%= cat %>" <%= product.category === cat ? 'selected' : '' %>><%= cat %></option>
                                <% } %>
                            </select>
                        </div>
                        <div class="col-sm-6">
                            <label class="form-label">Badge <span class="text-muted">(optional)</span></label>
                            <select name="badge" class="form-select">
                                <option value="">No badge</option>
                                <% for (const badge of ['Best Seller', 'New', 'Trending', 'Hot Pick', 'Summer Deal', 'Eco Pick', 'Limited', 'Outdoor']) { %>
                                <option value="<%= badge %>" <%= product.badge === badge ? 'selected' : '' %>><%= badge %></option>
                                <% } %>
                            </select>
                        </div>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary px-4" style="border-radius:10px;"><i class="bi bi-check-circle me-2"></i>Save Changes</button>
                        <a href="/admin-panel/dashboard" class="btn btn-light px-4" style="border-radius:10px;">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<%- include('_footer') %>
<script>
function previewImage(input) {
    if (input.files && input.files[0]) {
        const reader = new FileReader();
        reader.onload = function(e) {
            document.getElementById('dropzoneContent').style.display = 'none';
            const img = document.getElementById('previewImg');
            img.src = e.target.result; img.style.display = 'block';
        };
        reader.readAsDataURL(input.files[0]);
    }
}
function handleDrop(e) {
    e.preventDefault();
    document.getElementById('dropzone').style.borderColor = '#e2e8f0';
    const file = e.dataTransfer.files[0];
    if (file && file.type.startsWith('image/')) {
        const input = document.getElementById('imageInput');
        const dt = new DataTransfer(); dt.items.add(file); input.files = dt.files;
        previewImage(input);
    }
}
</script>
</body>
</html>
